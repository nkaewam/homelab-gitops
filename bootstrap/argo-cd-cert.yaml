apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
    name: argo-cd-tls
    namespace: argocd
spec:
    issuerRef:
        name: selfsigned-issuer
        kind: ClusterIssuer
    dnsNames:
        - "argo-cd.nkaewam.local"
